# 1.0.72 联动发布记录

本记录随发布候选提交；“本地验证”不代表生产已部署。最终以 GitHub 工作流、Release 签名清单和运行检查为准。

## 交付范围

- 客户端：逐窗口采集会话、帧序号、同帧 OCR 证据；停止/离开与视觉清空区分；启动不重放停用期事件；
  统一状态模式下本地裸人数不覆盖服务器选择；未知状态显示“上次”，内容不变不重排浮窗。
- 服务端：按连续驻留选单主、截断接管；星系当前状态和事件同事务；独立过期维护、失败写入修复、查询隔离。
- 档案与组织关系：长期档案批量处理、官方剩余期限、分级后台刷新；独立军团/联盟来源快照；无个人声望例外；
  同军团/有效联盟按 ID 识别、显式零关系保留、失败不当空表、授权切换隔离；管理配置无需手动重启。
- ESI 路线：114 共享连接与预算，47 固定目标 TLS 透传；受保护的分阶段切换及回滚，不删除旧缓存。
- 前端/QQ：消费同一当前状态、未知状态不重复通知、名单采用最新人数；更新设计、API、介绍和唯一客户端指南入口。

## 本地验证记录

- 服务端全量：846 通过、8 跳过（未提供测试 PostgreSQL 时跳过真实库模块，排除用户原有未跟踪压测材料）。
- 真实隔离 PostgreSQL：状态/关系/批量专项 15 通过；新增停止心跳和独立频道边界后，完整状态专项 9 通过。
- Windows 客户端：366 通过；独立 client-server API 进程 71 通过。
- 前端：25 个测试文件、124 用例通过，TypeScript/生产构建通过。
- Gateway：55 通过、3 跳过；机器人全量通过、Ruff 通过。
- 新增转发指标/传输组合 16 通过；切换文件修改测试通过，POSIX 回滚测试由 Linux CI 验证。
- Qt 离屏截图检查未知状态标签，回归检查启动及内容不变时的布局尺寸。

分批结果有重叠，不相加当作独立用例总数。后续最终复跑与生产验收追加到历史台账，不追改旧记录。

提交前最终复跑：服务端 848 通过/9 跳过；完整真实 PostgreSQL 发布门禁 92 通过；
工作流 YAML 解析、Python 编译、新增运行模块 Ruff 和暂存区 whitespace 检查通过。
GitNexus 暂存区扫描为 99 文件、617 符号、50 流程，整体 critical，已作风险提示并核对实际 Git 文件范围；
索引存在同名文档/符号误关联，SQL/YAML 与部分新文件另以直接 diff 和测试复核，不把图谱扫描视为完整证明。

首次 GitHub PostgreSQL 门禁 91 通过/1 失败，拦截了服务器部署：旧归属更正用例用未来 1～2 秒时间，
较快的 Linux CI 尚未到达该时间，触发正确的未来资料拒绝规则。修正测试使用已过去的递增时间，
另补固定时钟用例验证未来资料仍不可信；未修改生产有效期或绕过门禁。客户端、Gateway、机器人、协议及下载站流水线已通过各自验证。

第二次服务端流水线 PostgreSQL 门禁通过；Windows 全量测试 848 通过、9 跳过、1 失败，
失败为不可信 TLS 证书测试缺少 `cryptography` 测试依赖。仅补齐 CI 测试安装项，保留证书拒绝断言，
不修改生产 TLS 校验或增加运行时依赖。服务端部署仍被门禁拦截，待新流水线通过。

## 2026-09-13 实际发布结果

- 客户端 [v1.0.72](https://github.com/xiaqijun/eve-sentry/releases/tag/v1.0.72) 已正式发布，
  [发布流水线](https://github.com/xiaqijun/eve-sentry/actions/runs/34710406258) 成功，下载站 `latest.json` 已核对为 1.0.72。
  客户端源提交为 `cc1029cbe7df72b96d99cca0354eb2c3b401a1df`；后续两次修正只涉及服务端 CI/测试与台账。
- [服务端部署](https://github.com/xiaqijun/eve-sentry/actions/runs/34710947332) 全部通过：Windows Python 849 通过/9 跳过，
  PostgreSQL 92 通过，前端 25 文件/124 用例通过，构建及公网 readiness 通过。
- [Gateway 部署](https://github.com/xiaqijun/eve-sentry/actions/runs/34710953305) 成功；114 和 47 的部署标记均为
  `bb5790fa9030a099212d5401f9f393a71b6ee8ab`，服务 active。机器人已随 `cc1029c` 成功部署。
- [受保护的 ESI 切换](https://github.com/xiaqijun/eve-sentry/actions/runs/34711301033) 成功执行
  47 dual → 114 relay → 47 relay。114 的 mode 已读取确认为 relay；47 health 返回 relay，2 条隧道建立，
  旧 JSON 缓存路由及后台缓存队列停用，历史存储保留。以上为切换后短窗口检查，不代表长期无故障。
- 114 档案 mode=on，后台刷新/历史回填开启，并发上限 4；原有 1,046 份人员档案、431 个组织记录保留，
  刷新任务 2,938 条；`system_current_state` 已建立并有 69 行。行数为检查时快照，不等于在线节点数。
- 组织关系分类仍为默认 off，未自动开启；纯组织规则（无个人声望例外）仅在 organization_mode=on 生效。
  后续在管理界面先 shadow 核对授权、关系来源及比较结果，再 on。没有将生产配置默认为已启用。
- 切换后服务端 readiness 正常。最近 5 分钟（最多 3,000 条）日志按 ERROR/Traceback/timeout 等标记抽查为零，
  不是完整故障率统计；尚未执行真实游戏截图、双节点接管、远端清空与 QQ 实际投递的端到端时延验收。

## 2026-09-13 运行检查与后续修复（未部署）

13:03 CST 只读复核：生产仍运行 `bb5790f`，档案 on、组织模式 shadow（配置更新时间 12:43:15），
没有将 shadow 当作新分类已生效。1074 份档案中可信归属为 0；军团/联盟关系成功快照分别含 7/228 条，
两来源当时已授权且失败计数为 0。13:02 的一次联盟刷新失败随后恢复；这些是时间点快照，不代表长期失败率。

确认两个独立根因：旧父身份零 Presence 抢占同机子窗口主来源，使当前敌情未进入状态事件；
relay 归属响应缺少缓存头，被保守逻辑立即过期，导致人员分类待确认。
本地补丁排除并退役已被逐窗口替代的裸身份，保留重启迁移证据；归属 POST 使用官方声明的
3600 秒 TTL 补充缺失缓存头，仍保留响应年龄、显式限制和过期保护。补充脱敏失败原因日志，
没有删除旧档案/上报，也没有切换组织配置、提交推送或部署生产。

回归覆盖当前子窗口来敌/清空、旧错误状态重启对账、停止后立即重启、其他机器顺序不变、
Date/Age/显式过期限制、刷新后恢复可信且不立即重复入队、日志不泄露凭据。
新增数据库迁移回归纳入受保护服务端部署的 PostgreSQL 门禁。

本次本地验证：服务端 870 通过、10 跳过（未配置真实库的全量运行，排除用户原有未跟踪压测用例）；
隔离 PostgreSQL 完整门禁 95 通过。新增/相关模块 Ruff、Python 编译、工作流 YAML 和 diff whitespace 检查通过；
`personnel_runtime.py` 保留原有 B020 警告，本次该文件检查仅排除这一已有项。
GitNexus 对 main 的差异扫描覆盖 12 个已跟踪文件、30 个关联符号、3 条流程，medium；
另外 1 个新增诊断模块和 4 个新增测试文件以直接审阅与测试核对，未把未跟踪文件算入图谱扫描。
本地 PostgreSQL 验证后已停止；以上结果不代表真实生产时延验收完成。

### 同日追加：Relay 观测与影子统计（本地，未部署）

生产只读检查确认 114/47 服务均 active、开机自启 enabled；47 `/health` 返回 relay，
当时累计建立 632 条连接、当前 2 条隧道。页面“未启用远端网关”源于仅判断旧客户端 health 方法，
不是自动启动失败。新增有界、无令牌的 relay 健康检查，并区分直连；旧 Gateway 观测兼容。
页面不再把 relay 的业务缓存/请求延迟缺失显示成 0；探测失败时保留值标明是旧数据。

影子比较保留原累计口径兼容，新增有效比较、待确认和确定敌我差异；待确认不计入新差异率。
复现 2288 次全待确认后，新界面显示有效比较 0、待确认 2288、敌我差异 0、差异率暂无。
生产组织模式不自动改变，前一轮缓存/主来源修复也仍待统一推送部署。

本地服务端全量 889 通过/10 跳过，前端 25 文件/129 用例通过，TypeScript 与生产构建通过。
新增运行模块和用例 Ruff、diff whitespace 检查通过；浏览器模拟 relay、直连、不可达、待确认，
核对桌面及 375 像素宽页面无横向溢出。测试数据仅本地模拟，没有为截图访问生产接口。
GitNexus 累计工作树对 main 扫描为 20 已跟踪文件、45 关联符号、5 流程，medium；
新文件另外直接审阅和测试。构建保留既有大包、依赖 use-client 等提示，不影响构建成功。

## 发布操作参考

推送 main 后等待服务端、机器人、Gateway、Client CI/客户端发布、协议和下载站工作流完成。
只有完成新版部署后才运行 `ESI transport rollout`，确认 114 档案可用再关闭 47 成功缓存路线。
组织规则先 shadow 比较，再 on；档案模式与网络模式独立。
客户端回滚使用此前签名版本；服务器回滚既有部署备份。数据库新增表保留，不做破坏性降级。
确认截图到服务端、SSE 到客户端、QQ 排队与实际发送各阶段时延；未采样的真实运行指标不填写推测值。
