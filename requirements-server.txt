# Server-only deployments intentionally avoid the desktop OCR dependencies
# from requirements.txt. The current intel server path uses Python stdlib only.
#
# Keep this file so Linux or container deployments can run:
#   python -m pip install -r requirements-server.txt
#
# Optional ESI and zKillboard integrations also use the standard library.
PyYAML>=6.0
